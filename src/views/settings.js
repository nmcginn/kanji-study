export function settingsView() {
  return `
    <section>
      <h2>Settings</h2>
      <p style="color:var(--color-text-muted);margin-top:var(--spacing-sm)">
        Deck ordering preferences, progress reset, and display options. Coming soon.
      </p>
    </section>
  `;
}
