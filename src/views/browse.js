export function browseView() {
  return `
    <section>
      <h2>Browse Kanji</h2>
      <p style="color:var(--color-text-muted);margin-top:var(--spacing-sm)">
        Filter and sort kanji by JLPT level, RTK order, stroke count, and more.
        Coming soon — data layer in progress.
      </p>
    </section>
  `;
}
