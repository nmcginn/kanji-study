export function drillView() {
  return `
    <section>
      <h2>Drill</h2>
      <p style="color:var(--color-text-muted);margin-top:var(--spacing-sm)">
        Flashcard drill mode — kanji to keyword and keyword to kanji.
        Coming soon — requires browse + data layer first.
      </p>
    </section>
  `;
}
